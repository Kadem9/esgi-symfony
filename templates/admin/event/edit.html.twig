{% extends 'base_admin.html.twig' %}

{% block body %}
    <div class="flex justify-between items-center mb-1">
        <h1 class="h3">Modifier un évènement</h1>
        <a href="{{ path('admin_event_index') }}" class="btn btn-secondary">{{ icon('times') }} Annuler</a>
    </div>
    <div class="card mt-3">
        <div class="card-body">
            {% include "admin/event/_form.html.twig" %}
        </div>
    </div>
{% endblock %}